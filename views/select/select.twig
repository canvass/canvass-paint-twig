{% include 'field/field_open.twig' %}

    <select id="{{ field.identifier }}" class="{{ field.classes|default('') }}"
        name="{{ field.name }}" {% include 'attributes/attributes.twig' %}
    >
    {% for index, child in field.children %}
        {% include 'option/option.twig' %}
    {% endfor %}
    </select>

{% include 'field/field_close.twig' %}
